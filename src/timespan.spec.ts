import TimeSpan, { TimeSpanComponent } from "./timespan";

describe("Comparsions", () => {
    describe("TimeSpan TimeSpan Comparision", () => {
        it("Equality", () => {
        //     const anHour = new TimeSpan(1,0,0)
        //     const anHourAndAMinute = new TimeSpan(1,1,0)
        //     expect(anHour.equals(anHourAndAMinute, TimeSpanComponent.hours && TimeSpanComponent.minutes)).toBeFalsy()
        });
    });
});